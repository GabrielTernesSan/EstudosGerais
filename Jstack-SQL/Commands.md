Docker start (nome do container) // Inicia um container já criado

Docker compose down // Destroi o container 

Docker compose up // Cria o container

Docker exec -it (container) Bash

psql -f (arquivo sql)